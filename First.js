// var can be re-declared and re-initalized
// let cannot be re-declared but can be re-initialized
// const cannot be re-declared and re-initialized
// let and const are blocked scope (cannot access outside )
// var is function scope

//  datatypes
// primitive : number(big int),string,boolean,null,undefine,array
// non-primitive : object,classes

// operator
// airthematic: +,-,*,/,%,++,--
// comparison: ==,===,!=,!==,<,>,>=,<=
// assignment: +=,**=

// == it will compare the value without considering datatype
// === it will compare the value without considering the datatype

function hello() {
  console.log("my first function");
}
hello();
